def solve():
    A = np.array([[10, 1, 1],
                  [2, 10, 1],
                  [2, 2, 10]], dtype=float)

    B = np.array([45, 130, -44], dtype=float)

    solution = gauss_seidel(A, B)
    result_label.config(text="Soln: " + str(np.round(solution, 6)))
